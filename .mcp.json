{
  "mcpServers": {
    "notion-attachments": {
      "command": "uv",
      "args": ["run", "--with", "fastmcp", "--with", "requests", "python", "${CLAUDE_PLUGIN_ROOT}/notion_attachment_urls_mcp.py"],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      }
    }
  }
}
